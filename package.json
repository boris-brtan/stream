{
  "name": "stream",
  "version": "0.1.0",
  "description": "Streaming PWA",
  "private": true,
  "scripts": {
    "build": "NODE_ENV=production npx esbuild src/index.js --bundle --minify --outfile=dist/main.js",
    "clean": "rm -f dist/main.*",
    "start": "npm run clean && npx esbuild src/index.js --bundle --servedir=dist --outfile=dist/main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/boris-brtan/stream.git"
  },
  "keywords": [
    "STREAM",
    "AUDIO",
    "VIDEO",
    "JS"
  ],
  "author": "boris-brtan",
  "license": "Unlicense",
  "bugs": {
    "url": "https://github.com/boris-brtan/stream/issues"
  },
  "homepage": "https://github.com/boris-brtan/stream#readme",
  "devDependencies": {},
  "dependencies": {}
}
